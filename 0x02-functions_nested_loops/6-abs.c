#include "main.h"
/**
 * _abs - return the absolute of an integer.
 *
 * @n: the argument value.
 *
 * Return: returns the absolute value.
 */
int _abs(int n)
{
	if (n >= 0)
	{
		return (n);
	}
	else
	{
		return (-n);
	}
}
