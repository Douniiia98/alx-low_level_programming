#include "main.h"
/**
 * add - returns the sum of two integers.
 * @x: argument
 * @y: argument
 * Return: returns the addition value of the two arguments
 */
int add(int x, int y)
{
	return (x + y);
}
