#include <stdio.h>

/**
 * main - prints the following string "with proper grammar, but the ... "
 *
 * Return: zero if working correctly
 */
int main(void)
{
	printf("with proper grammar, but the outcome is a piece of art,\n");
	return (0);
}
