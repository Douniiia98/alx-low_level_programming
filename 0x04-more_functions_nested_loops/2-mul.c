#include "main.h"

/**
 * mul - multiplies two integers
 * @a: first variable
 * @b: second variable
 * Return: the result of mul(a,b).
 */
int mul(int a, int b)
{
	int s;

	s = a * b;

	return (s);
}
